# AChat-App
Chat app using socket.io and JavaScript
# Install dependencies 
After download or clone run `npm install` to install all the dependencies.
To run the code:
1) Goto AChat App folder
2) Run node server.js
